[project]
name = "axiom-de-rd"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34.0",
    "datalab-python-sdk",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.setuptools.package-data]
"src.ingestion.parsing.lift" = ["schemas/*.json"]
