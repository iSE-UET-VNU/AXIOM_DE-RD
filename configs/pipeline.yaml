{
  "input_dir": "data/raw",
  "output_dir": "data/processed",
  "enabled_modules": [
    "ingestion",
    "cleaning",
    "enrichment",
    "indexing_cataloging",
    "integration",
    "storage"
  ],
  "storage": {
    "mode": "local",
    "local": {
      "artifacts_dir": "data/processed/artifacts"
    }
  },
  "logging": {
    "level": "INFO"
  }
}
